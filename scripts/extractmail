#!/usr/bin/env bash
# Stable host wrapper → python/extractmail_stdin.py
set -euo pipefail
ROOT=$(cd "$(dirname "$0")/.." && pwd)
export PYTHONPATH="$ROOT/python${PYTHONPATH:+:$PYTHONPATH}"
exec python3 "$ROOT/python/extractmail_stdin.py" "$@"
